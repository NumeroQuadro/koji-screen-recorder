# Update Flow Checklist (Sparkle) — Kōji

Note: `SUFeedURL` is a placeholder by default, so “Check for Updates…” may show a connection error — that’s expected.

## Checklist

- [ ] "Check for Updates" menu item is present
- [ ] Clicking it shows "checking..." then "up to date" (or connection error if no appcast)
- [ ] Settings > Updates tab has toggle and check button
- [ ] App doesn't crash on update check failure
